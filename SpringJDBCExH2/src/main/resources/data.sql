CREATE TABLE IF NOT EXISTS student (
                                       id INT PRIMARY KEY,
                                       name VARCHAR(255),
    marks INT
    );

INSERT INTO student (id, name, marks) VALUES (2, 'kiran', 79);
